
$('.autoplay').slick({
  slidesToShow: 1,
  slidesToScroll: 1,
  autoplay: true,
  autoplaySpeed: 2000,
});

if (totalSpent >= 50000) { discount = SILVER_DISCOUNT }
if else (totalSpent >= 20000 && totalSpent < 50000){ discount = SILVER_DISCOUNT } if else (totalSpent >= 5000 && totalSpent < 20000){ discount = BRONZE_DISCOUNT } else { BASE_DISCOUNT }
